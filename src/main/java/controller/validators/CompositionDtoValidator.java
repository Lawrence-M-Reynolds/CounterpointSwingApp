package controller.validators;

import userInterface.dto.CompositionDTO;

/**
 * Validates the CompositionDTO when creating/editing
 * a Composition.
 * 
 * Not implemented yet. Just created so that I can
 * do so in future if necessary.
 * @author L Reynolds
 *
 */
public class CompositionDtoValidator {

	/**
	 * Validates the CompositionDTO. At the moment
	 * just returns a boolean and wouldn't say
	 * what is wrong. This can be changed in future.
	 * @param compositionDTO
	 * @return
	 */
	public boolean validateCompositionDTO(
			CompositionDTO compositionDTO){
		return true;
	}
}
